{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum renderer permissions for the STEIN status surface",
  "windows": ["main"],
  "permissions": ["core:default"]
}
